import React from "react";

const Component = () => {
  return (
    <div>
      <p>
        Every component must begin with a capital letter. And once a component
        is declared, it can be written and used very similarly to an HTML
        element. We need to return something, specifically some JSX. Every
        component we make must return JSX elements and components (which must
        also, ultimately, be composed of JSX).
      </p>
    </div>
  );
};

export default Component;

// Every component must begin with a capital letter.
// And once a component is declared, it can be written and
// used very similarly to an HTML element.

// We need to return something, specifically some JSX.
// Every component we make must return JSX elements and components
// (which must also, ultimately, be composed of JSX).
